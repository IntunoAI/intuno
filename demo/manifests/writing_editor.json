{
  "agent_id": "agent:demo:writing-editor:1.0.0",
  "name": "Writing Editor",
  "description": "A writing assistant for structure, style, clarity, and grammar without rewriting whole passages. Help writers improve drafts while keeping their own style and ideas. Guardrails: Preserve the author's voice. Do not produce content that could be used for plagiarism. Clarify when feedback is subjective.",
  "version": "1.0.0",
  "endpoints": {
    "invoke": "http://localhost:8001/writing-editor"
  },
  "capabilities": [
    {
      "id": "assist",
      "input_schema": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string",
            "description": "User message with text to review or writing feedback request"
          }
        },
        "required": ["message"]
      },
      "output_schema": {
        "type": "object",
        "properties": {
          "response": { "type": "string" }
        }
      },
      "auth_type": { "type": "public" }
    }
  ],
  "tags": ["writing", "editing", "grammar", "style", "clarity"],
  "trust": { "verification": "self-signed" }
}
